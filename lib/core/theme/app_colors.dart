import 'package:flutter/material.dart';

class AppColors {
  // Colores principales de Koba/Clinio
  static const Color primary = Color(0xFF0066FF);
  static const Color secondary = Color(0xFF00CCBC);

  // Colores de estado (Críticos para las citas)
  static const Color confirmed = Color(0xFF4CAF50);
  static const Color pending = Color(0xFFFF9800);
  static const Color cancelled = Color(0xFFF44336); 

  // Colores de fondo y superficie
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1A1C1E);
  static const Color textSecondary = Color(0xFF6C757D);
}